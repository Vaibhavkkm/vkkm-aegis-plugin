# ── Core web framework ─────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# ── Quantitative math (Phase 2 / v2.0) ─────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0

# ── Live market data (Phase 3 / v3.0) ───────────────────────────────────
yfinance>=0.2.37

# ── ML Probability of Default & Sentiment (Phase 3 / v3.0 / v4.0) ──────────
scikit-learn>=1.4.0
joblib>=1.3.0
pandas>=2.2.0
transformers>=4.38.0
torch>=2.2.0

# ── Excel/PDF/JSON export (Phase 3 / v3.0 / v4.0) ────────────────────────
openpyxl>=3.1.0
reportlab>=4.1.0
matplotlib>=3.8.0
seaborn>=0.13.0

# ── File upload support (used by /backtest CSV upload) ──────────────────
python-multipart>=0.0.9

# ── SQL Database Integration (Phase 4 / v4.0) ───────────────────────────
SQLAlchemy>=2.0.0

# ── API Requests (Phase 4 / v4.0) ───────────────────────────────────────
requests>=2.31.0
